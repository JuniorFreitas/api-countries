# Configuração da API
PORT=3000
NODE_ENV=development

# Configuração do CORS
# Domínios permitidos separados por vírgula
# Exemplo: https://meusite.com,https://app.meusite.com,http://localhost:3000
ALLOWED_ORIGINS=https://meusite.com,https://app.meusite.com,http://localhost:3000

# Para desenvolvimento local (opcional)
# Se não definido, usa os domínios padrão de desenvolvimento
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000
